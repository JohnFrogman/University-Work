#include <vector>
#include <string>
#include <sstream>
#include <algorithm>
#include <iterator>

main int()
{
	std::cout << "Enter filename";
	string filename;
	std::cin >> filename;
	concurences(filename);
	return 0;
}

concurences(string filename) vector<string>
{
	vector<string> tokens;
	for(int i = 0; i<filelength(filename); i++)
	{
		for(int j = 0l j<; j++)
		{
		}
	}
	return tokens;
}

filelength()

/*
def get_coocs(fname):
    T = {}
    with open(fname) as F:
        for line in F:
            feats =line.strip().split()[1:]
            for i in range(0, len(feats)):
                for j in range()
                if feats[i] !=feats[j]:
                    pair = (feats[i], feats[j])
                    if pair in T:
*/